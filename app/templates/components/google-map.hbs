<div class="map-canvas"></div>
<div class="markers" style="display: none;">
  <ul>
    {{#each _markers itemView='google-map/marker'}}
      <li>{{title}} @ {{lat}},{{lng}}</li>
      {{#if view.hasInfoWindow}}
        {{view 'google-map/info-window'}}
      {{/if}}
    {{/each}}
  </ul>
  <ul>
    {{each _infoWindows itemView='google-map/info-window'}}
  </ul>
</div>
