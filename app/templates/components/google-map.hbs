<div class="map-canvas"></div>
<div style="display: none;">
  <ul>
    {{#each _markers itemView=markerViewClass}}
      <li>{{title}} @ {{lat}},{{lng}}</li>
      {{#if view.hasInfoWindow}}
        {{view 'google-map/info-window'}}
      {{/if}}
    {{/each}}
  </ul>
  <ul>
    {{each _infoWindows itemView=infoWindowViewClass}}
  </ul>
  <ul>
    {{each _polylines itemView=polylineViewClass}}
  </ul>
  <ul>
    {{each _circles itemView=circleViewClass}}
  </ul>
</div>
